<h1> Create new list </h1>

<%= simple_form_for @list do |t|%>
  <%= t.input :name %>
  <%= f.input :photo, as: :file %>
  <%= t.submit 'Create new list' %>
<% end %>


<%= link_to "Back", lists_path  %>
